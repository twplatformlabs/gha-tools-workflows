# workflow action hooks

The workflows maintained in this repo have references to specifically named local actions so that, when using the shared workflow in other repos, you can refer to those names to run actions from your local repo within the workflow. This is a worflow `hook` that enables the user to inject actions into the workflow, customizing the flow without needing to alter the source.  
